{
  "name": "Bella",
  "version": "1.0.0",
  "scripts": {
    "backend": "cd backend && npm install && node server.js",
    "frontend": "cd frontend && npm install && npm run dev -- --host 0.0.0.0 --port 8080",
    "dev": "concurrently -n BACKEND,FRONTEND -c green,cyan \"npm run backend\" \"npm run frontend\"",
    "start": "npm run dev"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
