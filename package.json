{
  "name": "app-loja",
  "version": "1.0.0",
  "scripts": {
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "backend": "cd backend && node server.js",
    "frontend": "cd frontend && npx vite --host 0.0.0.0 --port $PORT",
    "dev": "npm run install-all && concurrently -n BACKEND,FRONTEND -c green,cyan \"npm run backend\" \"npm run frontend\"",
    "start": "npm run dev"
  },
  "dependencies": {
    "concurrently": "^8.0.0"
  }
}