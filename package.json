{
  "name": "app-loja",
  "version": "1.0.0",
  "scripts": {
    "backend": "cd backend && npm install && node server.js",
    "frontend": "cd frontend && npm install && vite --host 0.0.0.0 --port $PORT",
    "dev": "concurrently -n BACKEND,FRONTEND -c green,cyan \"npm run backend\" \"npm run frontend\"",
    "start": "npm run dev"
  },
  "dependencies": {
    "concurrently": "^8.0.0"
  }
}